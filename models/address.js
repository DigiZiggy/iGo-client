export default class Address {
  constructor(id, addressLine1, addressLine2, city, state, postcode) {
    this.id = id;
    this.addressLine1 = addressLine1;
    this.addressLine2 = addressLine2;
    this.city = city;
    this.state = state;
    this.postcode = postcode;
  }
}

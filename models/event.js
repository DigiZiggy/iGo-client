export default class Event {
  constructor(id, activityTypes, title, description) {
    this.id = id;
    this.activityTypes = activityTypes;
    this.title = title;
    this.description = description;
  }
}
